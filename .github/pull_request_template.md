## 变更说明

说明用户可见行为、影响边界和明确不包含的内容。

## 风险与约束

- [ ] 已评估租户隔离、认证、幂等、审计、限流和健康检查的影响。
- [ ] 数据库变更仅包含新增 Flyway migration，并已确认兼容性与恢复方案。
- [ ] OCR 变更仍位于 `HandwrittenBillOcrProvider` SPI 边界，未记录供应商响应或凭据。
- [ ] 小程序变更保持服务层和 Safe Area 边界。

## 验证证据

- [ ] 已执行 `sh scripts/ai-harness-preflight.sh <base-ref>`。
- [ ] 已执行相关自动化验证，并记录结果。
- [ ] 小程序变更已在微信开发者工具验证加载、空数据、失败、保存中和 Safe Area 状态。
- [ ] 已执行 `git diff --check` 并审查最终差异。

## 未执行项

列出依赖环境、人工审批或后续发布流程的检查；没有则填写“无”。
