<view class="app-nav" style="{{navStyle}}">
  <button class="ui-button app-nav-back" wx:if="{{showBack}}" bindtap="onBack">
    <image class="app-nav-back-icon" src="{{backIcon}}" mode="aspectFit" />
  </button>
  <view class="app-nav-title">{{title}}</view>
</view>
