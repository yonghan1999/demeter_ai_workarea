<view class="state-panel">
  <view class="state-panel-title">{{title}}</view>
  <text wx:if="{{subtitle}}" class="state-panel-subtitle">{{subtitle}}</text>
  <button class="ui-button state-panel-action" wx:if="{{actionText}}" bindtap="onAction">{{actionText}}</button>
</view>
