<view class="status-segmented {{variant}}">
  <button class="ui-button status-option {{item.key === value ? 'active' : ''}}" wx:for="{{options}}" wx:key="key" data-status="{{item.key}}" bindtap="onSelect">{{item.text}}</button>
</view>
